import { UnavailableSurface } from "@/components/personal/unavailable-surface";
export default function AutomationPage() { return <UnavailableSurface title="Automation is unavailable" description="No automation provider is configured for this workspace. Connect an integration before workflows can be created or executed." />; }
