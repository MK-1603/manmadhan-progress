"use client";
import { useAuth } from "@/components/auth/auth-context";
export default function ProfilePage() { const { user } = useAuth(); return <div className="min-h-full p-6 md:p-10"><div className="mx-auto max-w-2xl"><p className="text-[11px] font-bold uppercase tracking-widest text-muted-foreground">Account</p><h1 className="mt-2 text-3xl font-bold">Profile</h1><div className="mt-8 rounded-2xl border border-border bg-card p-6"><div className="flex items-center gap-4"><div className="flex h-14 w-14 items-center justify-center rounded-full bg-gold text-lg font-bold text-background">{(user?.name || user?.email || "U").slice(0, 1).toUpperCase()}</div><div><p className="font-semibold">{user?.name || user?.displayName || "Personal User"}</p><p className="text-sm text-muted-foreground">{user?.email || "No email available"}</p></div></div><p className="mt-6 text-sm text-muted-foreground">Profile preferences are managed from Settings.</p></div></div></div>; }
